using DataGen.Types.Name;

namespace DataGen.Types {
    public static class Factory {
        //public static FileBasedData<T> FileBasedData<T>(FileBasedData<T> dataTypeFactory)
        //    => dataTypeFactory;
    }
}